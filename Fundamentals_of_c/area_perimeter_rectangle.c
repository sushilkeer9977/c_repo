// find the area and perimeter of rectangle

#include<stdio.h>

main()
{
    float l = 78.9, b = 35.6, a, p;

    a = l*b;
    p = 2 * l+b;

    printf("area of rectangle is: %f", a);
    printf(" \n perimeter of rectangle is %f", p);
}