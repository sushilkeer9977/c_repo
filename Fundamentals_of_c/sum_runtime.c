#include<stdio.h>

main()
{
    int x, y, z;

    printf("Enter first number:");
    scanf("%d", &x);
    printf("Enter a second number:");
    scanf("%d", &y);
    z = x+y;
    printf("sum is:%d", z);
}