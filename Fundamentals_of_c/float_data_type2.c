// wap to program to print the students persentages(with the help of float data type)

#include<stdio.h>

main()
{
    float a = 34.6, b = 89.4, c = 67.89; // initialization float value in variable.

    printf("student A prs is: %.3f", a); // (.3) is denote - how many number print after decimal(print 3 number after deciimal)
    printf(" \n student B prs is: %.2f", b);
    printf(" \n student C prs is: %.4f", c);
}